import React from 'react';
import Layout from '../components/Layout';

const Home = (props) => {
    return(
        <Layout sidebar>
            Home
        </Layout>
    );
}

export default Home;
