import React from 'react';
import Layout from '../components/Layout';

const Orders = () => {
    return (
        <Layout sidebar>
            Orders
        </Layout>
    );
}

export default Orders